const initialPopulation = [
    {
        bdx: Math.random() * 2 * (Math.floor(Math.random() * 2) == 1 ? 1 : -1),
        bdy: Math.random() * 2 * (Math.floor(Math.random() * 2) == 1 ? 1 : -1)
    }
]

export default initialPopulation